{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {generatepy = id; load = id; save = id; saveAs = id; scan = id; }; 
            CLASS = SignatureController; 
            LANGUAGE = ObjC; 
            OUTLETS = {busyPanel = NSPanel; window = NSWindow; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}