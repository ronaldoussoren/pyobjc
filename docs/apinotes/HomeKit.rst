.. module:: HomeKit
   :platform: macOS 26.4+
   :synopsis: Bindings for the HomeKit framework

API Notes: HomeKit framework
=============================

The full API is described in `Apple's documentation`__, both
the C and Objective-C APIs are available (but see the `API Notes`_ below).

.. __: https://developer.apple.com/documentation/homekit/?preferredLanguage=occ

These bindings are accessed through the ``HomeKit`` package (that is, ``import HomeKit``).

.. macosadded:: 26.4

.. note::

   This is an experimental binding based on the HomeKit headers for iOS.

API Notes
---------
