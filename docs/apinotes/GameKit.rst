.. module:: GamkeKit
   :platform: macOS
   :synopsis: Bindings for the GamkeKit framework

API Notes: GameKit framework
============================

The full API is described in `Apple's documentation`__, both
the C and Objective-C APIs are available (but see the `API Notes`_ below).

.. __: https://developer.apple.com/documentation/gamekit/?preferredLanguage=occ

These bindings are accessed through the ``GameKit`` package (that is, ``import GameKit``).

API Notes
---------
